name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ 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}}
          GIST_ID: ${{ GIST_TOKEN }}
          TIMEZONE: Asia/Seoul
